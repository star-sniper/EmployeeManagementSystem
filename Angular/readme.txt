Angular version of the app!

To run just go to directory level and type command "ng serve --o"

You make make changes to the files (if you want to deploy your version), but remember to build the app again 
through "ng build"

Check out the running version https://employees-system.herokuapp.com/home
